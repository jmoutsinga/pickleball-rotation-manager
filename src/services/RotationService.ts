import type { Game } from '@/models/Game'

export class RotationService {
  planNextRotation(currentGames: readonly Game[]): Game[] {
    void currentGames
    return []
  }
}

export default new RotationService()
