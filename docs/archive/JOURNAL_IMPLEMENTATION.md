# Journal d’implémentation archivé

Ce fichier conserve les anciennes entrées décrivant une exécution, une validation ou une clôture de tranche.
Il n’est pas nécessaire de le lire au démarrage d’une tâche. L’ancien plan complet est conservé dans
[`PLAN_AVANT_REORGANISATION_2026-08-20.md`](PLAN_AVANT_REORGANISATION_2026-08-20.md).

Les entrées sont classées de la plus récente à la plus ancienne. Certaines anciennes entrées combinaient
décision et compte rendu ; elles peuvent donc également apparaître dans le registre des décisions afin de ne perdre aucun contexte.

## Historique

- **2026-08-20 03:51:13** — Clôturer définitivement 3.20.6 et 3.20 après correction de l'ordre des effets. `RotationGameNumberMigration.prepare()` produit désormais la collection migrée uniquement en mémoire ; `migrate()` conserve la persistance immédiate attendue lors d'une lecture normale, tandis que `SessionGraphPersistenceService` utilise `prepare()` et diffère la migration jusqu'à sa propre sauvegarde post-validation. Un test de régression prouve qu'un graphe invalide ne modifie même pas une Rotation historique à numéroter. Nouvelle validation complète réussie : contrôle TypeScript, lint, 249 tests Vitest répartis dans 38 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite avec 101 modules. Aucun problème restant n'est détecté ; les avertissements npm `min-release-age` et Git LF/CRLF restent informatifs.
- **2026-08-20 03:49:37** — Rouvrir brièvement 3.20.6 après la revue de l'ordre des effets. `SessionGraphPersistenceService` appelle actuellement la migration persistante des numéros avant `validateSessionGraph()` ; une commande de graphe invalide pourrait donc corriger une ancienne Rotation avant d'être refusée. Conserver `migrate()` comme lecture migrante persistante pour les consommateurs historiques, mais ajouter une préparation en mémoire utilisée par le service de graphe, dont la sauvegarde des Rotations déjà prévue matérialisera la migration uniquement après validation. Justification : rendre exacte la garantie « aucune écriture du graphe avant validation » sans modifier le contrat public de `storageService.getRotations()`.
- **2026-08-20 03:48:38** — Clôturer 3.20.6 et l'ensemble de 3.20 sans anomalie détectée. Validation complète : contrôle TypeScript, lint, 249 tests Vitest répartis dans 38 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production avec 101 modules réussissent. La revue des dépendances confirme que `LocalStorageGateway` reste le seul code de production manipulant directement Web Storage et JSON, que les accès JSON bruts Session/Rotation sont limités aux migrations, et que `storageService` demeure une façade compatible de 144 lignes. Les avertissements npm `min-release-age` et Git LF/CRLF restent informatifs. La façade n'est pas supprimée : les stores Pinia continueront à migrer progressivement vers les services fins lors de tranches ultérieures.
- **2026-08-20 03:47:26** — Terminer 3.20.5. `RotationGameNumberMigration` et `SessionAttendeeMigration` possèdent désormais les deux compatibilités historiques ; `SessionPersistenceService` compose la lecture migrée, l'upsert et l'unicité d'une Session `STARTED`; `SessionGraphPersistenceService` fusionne les entités par identifiant, remplace les Teams de la Rotation ciblée, préserve l'historique, valide le graphe complet avant écriture et persiste les cinq collections sans toucher à la collection historique de Games. `storageService` passe de 317 à 144 lignes et conserve ses signatures pour Pinia. Validation intermédiaire : contrôle TypeScript, lint, 249 tests Vitest répartis dans 38 fichiers et `git diff --check` réussissent ; 57 tests ciblés couvrent directement les nouveaux services, la façade et les stores concernés. Démarrer 3.20.6 avec Cypress et le build de production.
- **2026-08-20 03:43:18** — Démarrer 3.20.5 après audit sans nouvelle incohérence de modèle. Extraire deux migrations idempotentes : reconstruction de `Session.attendingPlayers` et numérotation Session-wide des Games historiques. Introduire `SessionPersistenceService` pour composer la lecture migrée et l'unicité d'une Session `STARTED` par Location, puis faire dépendre `SessionGraphPersistenceService` des repositories et services fins pour fusionner les collections, valider le graphe avant toute écriture et le sauvegarder. `storageService` conserve toutes ses signatures publiques et devient une façade de délégation ; aucun store Pinia ni format JSON ne change. Justification : séparer compatibilité historique, cas d'usage Session et orchestration transverse comme des services applicatifs Spring, tout en gardant une frontière stable pour Vue.
- **2026-08-20 03:39:03** — Clôturer 3.20.4.4 après validation complète. `Session.fromJson()` restitue désormais fidèlement l'absence d'heure de démarrage d'une Session `CREATED`, directement et via `SessionRepository`; aucun correctif de données historiques n'est ajouté. Validation : contrôle TypeScript, lint, 241 tests Vitest répartis dans 34 fichiers, deux passages de 17 scénarios Cypress, `git diff --check` et build Vite de production avec 97 modules réussissent. Les avertissements npm `min-release-age` et Git LF/CRLF restent informatifs. La prochaine tranche est 3.20.5, consacrée aux migrations historiques et à `SessionGraphPersistenceService`.
- **2026-08-20 03:37:26** — Implémenter 3.20.4.4 par une modification unique de `Session.fromJson()` et deux assertions de contrat : réhydratation directe du modèle et aller-retour via `SessionRepository`. Les tests ont d'abord reproduit la création indue d'une date, puis les 11 tests ciblés réussissent après remplacement de cette valeur par `null`. La validation globale reste à exécuter avant de clôturer cette correction.
- **2026-08-20 03:33:02** — Clôturer 3.20.4.3 et l'ensemble de 3.20.4 après validation complète. Les six repositories d'entités sont extraits ; Session et Rotation exposent temporairement un accès JSON brut détaché uniquement pour les migrations encore orchestrées par la façade. Validation : contrôle TypeScript, lint, 241 tests Vitest répartis dans 34 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production avec 97 modules réussissent. Les avertissements npm `min-release-age` et Git LF/CRLF restent informatifs. Suspendre le passage à 3.20.5 le temps de valider la correction proposée de `Session.fromJson()` : préserver `null` pour une Session non démarrée au lieu de créer une date au rechargement.
- **2026-08-20 03:31:55** — Terminer l'implémentation de 3.20.4.3 avant validation globale, sans corriger une incohérence de modèle découverte pendant le test. `RotationRepository` et `SessionRepository` centralisent sauvegarde, JSON brut détaché et réhydratation simple ; la façade conserve les deux migrations contextuelles et passe à 317 lignes. Les 62 tests ciblés des repositories, migrations et stores réussissent. Incohérence soumise à décision : `Session.fromJson()` remplace actuellement un `startTime: null` par l'heure courante, de sorte qu'une Session `CREATED` rechargée semble temporellement démarrée malgré son statut. La tranche structurelle préserve provisoirement ce comportement ; aucune correction du modèle n'est incluse sans validation explicite.
- **2026-08-20 03:30:04** — Démarrer 3.20.4.3 avec une séparation explicite entre repository et migration. `SessionRepository` et `RotationRepository` posséderont la lecture/écriture de leur collection ainsi que la réhydratation simple. Ils exposeront temporairement les JSON bruts nécessaires aux migrations existantes, mais ne dépendront ni des autres repositories ni du graphe. La façade conservera la reconstruction historique de `attendingPlayers`, la migration Session-wide des numéros de Games et l'unicité d'une Session `STARTED` par Location. Justification : retirer la connaissance des clés et des primitives de collection sans attribuer aux repositories des règles qui seront déplacées en 3.20.5 dans les migrations et le service de graphe.
- **2026-08-20 03:26:55** — Clôturer 3.20.4.2 après validation complète. Les collections Court et Team sont désormais isolées derrière leurs repositories et la façade conserve intégralement son contrat. La conservation des identifiants de Courts après réduction/réaugmentation, les références historiques des Games et la réhydratation des Players de Team restent couvertes. Validation : contrôle TypeScript, lint, 237 tests Vitest répartis dans 32 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production avec 95 modules réussissent. Les avertissements npm `min-release-age` et Git LF/CRLF restent informatifs.
- **2026-08-20 03:25:53** — Terminer l'implémentation de 3.20.4.2 avant validation globale. `CourtRepository.saveAll/getAll` et `TeamRepository.saveAll/getAll` délèguent au gateway puis réhydratent systématiquement les modèles avec `Court.fromJson` et `Team.fromJson`. La façade ne connaît plus le format Team et conserve seulement l'usage de `Court` nécessaire à la validation transverse du graphe. Les fusions historiques restent inchangées dans `saveSessionGraph()`. Les 53 tests ciblés des repositories, de la façade, du validateur et des stores Session/Player réussissent. Justification : rendre le type retourné cohérent avec les autres repositories sans modifier identifiants, clés ou JSON persisté.
- **2026-08-20 03:24:39** — Démarrer 3.20.4.2. `CourtRepository` et `TeamRepository` posséderont uniquement la sauvegarde et la réhydratation de leurs collections via `LocalStorageGateway`. Les fusions par identifiant, la conservation des Courts/Teams historiques et la sélection des Teams remplacées resteront provisoirement dans `saveSessionGraph()`, car elles constituent une règle transactionnelle transverse et non un CRUD d'entité. La façade conservera `saveCourts/getCourts` et `saveTeams/getTeams`. Justification : éviter de pousser une règle de graphe dans un repository de collection tout en retirant de la façade la connaissance des formats JSON Court et Team.
- **2026-08-20 03:23:13** — Clôturer 3.20.4.1 après validation complète. `PlayerRepository`, `LocationRepository` et `StorageKeys` sont désormais les propriétaires de leurs responsabilités respectives ; la façade maintient les signatures historiques pour Pinia, les migrations et `sampleData`. Validation : contrôle TypeScript, lint, 233 tests Vitest répartis dans 30 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production avec 93 modules réussissent. Les avertissements npm `min-release-age` et Git LF/CRLF restent informatifs. La prochaine paire Court/Team restera séparée des règles transverses de `saveSessionGraph()` jusqu'à l'extraction du service de graphe.
- **2026-08-20 03:22:11** — Terminer l'implémentation de 3.20.4.1 avant validation globale. `StorageKeys` porte les sept clés inchangées, `PlayerRepository` centralise réhydratation, ajout et nom unique, mise à jour, fusion non destructive et transitions de statut, tandis que `LocationRepository` centralise réhydratation, ajout, mise à jour, filtrage actif et suppression logique. La façade délègue toutes ses méthodes Player/Location et passe de 409 à 320 lignes sans changement de consommateur. Les 34 tests ciblés des repositories, de la façade et des stores Player/Location réussissent. Justification : prouver les contrats directement sur chaque repository tout en conservant la couverture d'intégration historique.
- **2026-08-20 03:19:42** — Découper 3.20.4 en trois incréments et démarrer 3.20.4.1 avec Player et Location. Déplacer leurs opérations de collection, réhydratation, validations de type, unicité de nom et changements de statut dans deux repositories dédiés. Centraliser les clés dans `StorageKeys` afin que ni le gateway générique ni les repositories ne dépendent de la façade. `storageService` conservera chaque signature publique et déléguera, de sorte que les stores Pinia et `sampleData` restent inchangés. Justification : extraire d'abord deux ensembles autonomes et fortement couverts avant les entités participant aux migrations et au graphe Session.
- **2026-08-20 03:17:07** — Clôturer 3.20.3 après validation complète. La façade conserve les mêmes clés et signatures, tandis que `LocalStorageGateway` est le seul composant de la couche de persistance à manipuler les primitives Web Storage et JSON. Validation : contrôle TypeScript, lint, 226 tests Vitest répartis dans 28 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production avec 90 modules réussissent. Les avertissements npm `min-release-age` et Git LF/CRLF restent informatifs. La prochaine extraction 3.20.4 commencera par Player et Location afin de limiter les dépendances croisées avant Session/Rotation/Team/Court.
- **2026-08-20 03:16:03** — Terminer l'implémentation de 3.20.3 avant validation globale. `LocalStorageGateway.read/write/remove` centralise désormais la sérialisation JSON et les trois primitives de stockage. Toutes les méthodes de collection et `clearAll()` de la façade délèguent au gateway ; aucune référence à `localStorage` ou à `JSON.parse/stringify` ne subsiste dans `storageService`. Quatre tests de contrat couvrent valeur par défaut, copie JSON, suppression ciblée et conservation de l'erreur historique sur JSON invalide ; les huit tests ciblés du gateway et de la façade réussissent. Justification : verrouiller le comportement de l'adaptateur avant d'extraire les repositories d'entités.
- **2026-08-20 03:14:49** — Démarrer 3.20.3 comme extraction strictement structurelle. `LocalStorageGateway` possédera seul les appels `localStorage.getItem`, `setItem` et `removeItem`, ainsi que `JSON.parse`/`JSON.stringify`; il acceptera une clé opaque et une valeur par défaut sans connaître les clés applicatives ni les modèles. `storageService` conservera toutes ses méthodes publiques et déléguera au gateway. Justification : créer une frontière d'infrastructure testable avant de découper les responsabilités par entité, sans migration de données ni modification simultanée des stores.
- **2026-08-20 03:10:00** — Valider définitivement 3.20.1 et 3.20.2 : contrôle TypeScript, lint, 222 tests Vitest répartis dans 27 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production avec 89 modules réussissent. Les avertissements npm `min-release-age` et Git LF/CRLF restent informatifs. La tranche suivante est uniquement structurelle : extraire `LocalStorageGateway` sans modifier les modèles, le JSON ni le comportement des stores.
- **2026-08-20 03:08:37** — Clôturer 3.20.1 et 3.20.2. `Game` expose désormais des résultats en lecture seule, `recordScore()` met à jour les deux scores atomiquement et déduit le résultat hors égalité, `designateWinner()` résout uniquement une égalité avec une Team participante, et `isResolved` centralise l'état final. Une nouvelle saisie différente remplace le résultat précédent ; une resoumission strictement identique d'une égalité conserve sa désignation manuelle. `GameScoreService` limite ces commandes aux Rotations `SCORING`, et les actions Pinia délèguent puis sauvegardent le graphe. `Rotation.finish()` vérifie toutes les Games avant toute transition ou écriture de l'heure de fin. Validation intermédiaire : 66 tests ciblés répartis dans quatre fichiers réussissent. Justification : garder les invariants dans le domaine, le service applicatif sans responsabilité de composition et le store comme orchestrateur réactif/persistant.
- **2026-08-20 03:04:04** — Démarrer la tranche 3.20 avant la prochaine tranche d'interface. `Rotation.games` reste l'unique source de vérité de la composition. `Game` encapsulera ses scores et son résultat ; `GameScoreService` exposera uniquement la mise à jour atomique des deux scores et la désignation du gagnant lorsque les scores sont identiques. Pour des scores différents, gagnant et perdant seront déduits ; pour une égalité, la Game restera non résolue jusqu'à une désignation explicite. La concurrence adopte volontairement « le dernier appel valide gagne », sans version optimiste. `Rotation.finish()` vérifiera que toutes ses Games sont résolues avant de figer leur dernier état. En parallèle, `storageService` sera découpé progressivement en adaptateur localStorage, repositories/services d'entités et service de graphe, tout en restant provisoirement une façade. Justification : permettre des mises à jour de score ciblées sans déplacer la propriété de la composition hors de Rotation, puis réduire les responsabilités de persistance sans rupture globale des stores.
- **2026-08-20 02:15:09** — Clôturer la sous-étape 3.19.5 et l'ensemble de la tranche 3.19 selon le design validé. `Rotation.games` est l'unique source de vérité active : le service ne lit ni n'écrit plus la collection globale historique `pickleball_games`, qu'il laisse intacte pour compatibilité. La réhydratation attribue aux anciennes Games leurs numéros dans l'ordre Rotation/Court et persiste la migration dans les Rotations. `validateSessionGraph()` refuse avant exposition dans Pinia les références Court/Team inconnues, identifiants ou numéros réutilisés, séquences Rotation/Game incomplètes, Courts dupliqués, Teams partagées, Games multiples sur un Court et participants absents ou affectés plusieurs fois; une Session démarrée exige une Game par Court actif. Le store restaure la Rotation d'ordre maximal, conserve toutes les précédentes et réutilise les identifiants de Courts lors d'une réduction puis d'une augmentation du nombre actif. Validation finale : contrôle TypeScript, lint, 200 tests Vitest répartis dans 26 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production (88 modules) réussissent.
- **2026-08-20 02:06:05** — Clôturer la sous-étape 3.19.4. `Game` porte désormais un `number` immuable, entier strictement positif, sérialisé dans `Rotation.games`; les anciens JSON peuvent recevoir un numéro de migration. `Session.getNextRotationOrder(rotations)` et `Session.getNextGameNumber(rotations)` calculent `max + 1` exclusivement dans l'historique de la Session concernée. Le store attribue ces numéros lors de la création d'une Rotation et renumérote une Rotation encore `CREATED` depuis l'historique qui la précède; il sélectionne comme courante la Rotation au plus grand `order`. Validation intermédiaire : contrôle TypeScript, lint et 189 tests Vitest répartis dans 25 fichiers réussissent.
- **2026-08-20 02:03:31** — Clôturer la sous-étape 3.19.3. Le store Pinia expose désormais `startRotation(at)`, `startRotationScoring()` et `finishRotation(at)`, exige une Rotation chargée, délègue chaque transition et chaque horaire à l'agrégat `Rotation`, puis persiste le graphe. Les tests du store utilisent ces actions au lieu d'appeler directement le modèle et confirment que `setCourts`, `addPlayer`, `removePlayer` et `movePlayer` restent sans effet en `IN_PROGRESS`, `SCORING` et `FINISHED`. Validation intermédiaire : contrôle TypeScript, lint et 182 tests Vitest répartis dans 24 fichiers réussissent.
- **2026-08-20 01:46:27** — Autoriser la réalisation successive des sous-étapes 3.19.3, 3.19.4 et 3.19.5, puis suspendre l'implémentation après l'audit du graphe pour décision de design. `Game` possède actuellement deux sources de vérité persistantes — les objets embarqués dans `Rotation.games` et la collection globale `pickleball_games` — que `saveSessionGraph()` tente de synchroniser. Par ailleurs, plusieurs Rotations d'une Session peuvent être conservées, tandis que `ensureSession()` charge la première occurrence avec `find()` sans définir la Rotation courante. Justification : la numérotation séquentielle des Games et la validation/restauration d'un historique multi-Rotations ne doivent pas être construites sur une propriété et une sélection ambiguës.
- **2026-08-20 01:39:02** — Clôturer la sous-étape 3.19.2. `Rotation` expose désormais `startTime` et `endTime` en lecture seule, copie les objets `Date` pour protéger son état interne, renseigne le début avec `start(at)` et la fin avec `finish(at)`, conserve les horaires pendant `startScoring()` et refuse atomiquement une chronologie inversée. `RotationJson` sérialise les dates en ISO ou `null`; ses propriétés optionnelles permettent de restaurer les anciennes données sans horaires à `null`. Validation finale : contrôle TypeScript, lint, 180 tests Vitest répartis dans 24 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production (87 modules) réussissent.
- **2026-08-20 01:36:17** — Démarrer la sous-étape 3.19.2 avec le même contrat temporel que `Session` : représenter `Rotation.startTime` et `Rotation.endTime` par `Date | null` dans le domaine et par des chaînes ISO ou `null` dans le JSON. Encapsuler les deux valeurs derrière des getters, les renseigner uniquement par `start(at)` et `finish(at)`, conserver les horaires pendant `startScoring()`, refuser une fin antérieure au début sans mutation partielle et accepter les anciens JSON dépourvus des deux propriétés en les restaurant à `null`. Justification : garder statut et horaires sous la responsabilité de l'agrégat tout en préservant les données déjà enregistrées dans `localStorage`.
- **2026-08-20 01:31:04** — Clôturer la sous-étape 3.19.1. `Rotation.status` est désormais exposé en lecture seule par un getter adossé à `_status`; `start()`, `startScoring()` et `finish()` appliquent exclusivement la séquence `CREATED → IN_PROGRESS → SCORING → FINISHED` et refusent toute autre commande sans altérer l'état. Les fixtures du store font progresser leurs Rotations par ces commandes au lieu d'affecter directement le statut. Validation finale : contrôle TypeScript, lint, 178 tests Vitest répartis dans 24 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production (87 modules) réussissent.
- **2026-08-17 10:04:23** — Remplacer le README historique, qui présentait à tort la rotation automatique comme terminée, par une documentation publique alignée sur l'état réel du projet. Séparer les fonctionnalités opérationnelles de la feuille de route, expliciter la portée du `localStorage`, les cycles de vie métier, l'initialisation des exemples, les routes, les commandes de validation et la configuration Cloudflare Pages. Conserver `PLAN.md` comme source de vérité détaillée et le README comme synthèse destinée aux utilisateurs et contributeurs. Validation : liens relatifs présents, sections attendues vérifiées et `git diff --check` réussi sur le README ; aucune exécution applicative requise pour cette modification exclusivement documentaire.
- **2026-08-17 01:39:47** — Clôturer l'étape 1.27. Home propose une initialisation unique tant que `pickleball_sample_data_initialized` est absent ; le service fusionne sans écraser les données existantes, crée les cinq Locations et les cinquante Players demandés, puis rafraîchit immédiatement la grille. Le flag n'est posé qu'après les deux sauvegardes et masque durablement le bouton. Validation finale : contrôle TypeScript, lint, 168 tests Vitest répartis dans 24 fichiers, 17 scénarios Cypress, `git diff --check` et build Vite de production (87 modules) réussissent.
- **2026-08-17 01:38:36** — Intégrer le cas d'usage dans `HomeView` avec une `ref` locale qui pilote le rendu conditionnel du bouton et un rechargement explicite du store Location après succès. Le bouton intercepte son clic, précède le `h1`, reste disponible si le service lève une erreur et disparaît dès que le flag est posé. Ajouter un parcours Cypress qui contrôle le contenu créé, l'unicité, les statuts et la persistance après rechargement ; ce nouveau cas devra lui aussi posséder un miroir Playwright en 6.12. Validation intermédiaire : 29 tests Vitest ciblés, contrôle TypeScript, lint et 17 scénarios Cypress réussissent.
- **2026-08-17 01:33:37** — Implémenter en TDD le service `sampleData` : les cinq Locations et les trois listes de 20/15/15 prénoms sont construites par les builders existants, les collisions de noms sont ignorées sans modification des entités présentes, un second appel est refusé par le flag et une tentative en erreur restaure les deux collections antérieures avant de laisser le flag absent. Validation intermédiaire : 3 tests de service et contrôle TypeScript réussissent.
- **2026-08-16 15:45:42** — Clôturer l'ajustement 3.18.2. `SessionForm` ne fixe plus de `max-height` et ni le formulaire ni son conteneur de grille ne définissent d'`overflow`; leur hauteur suit donc les cartes et seul le document porte l'ascenseur vertical. L'en-tête sticky conserve le titre et « Start Session » à `top: 0`. Cypress utilise 20 Players pour provoquer un débordement réel, vérifie l'égalité `scrollHeight/clientHeight` du conteneur, le dépassement du document et la position visible de l'en-tête après scroll jusqu'en bas. Validation finale : contrôle TypeScript, lint, 163 tests Vitest répartis dans 23 fichiers, 16 scénarios Cypress et build Vite de production (86 modules) réussissent.
- **2026-08-16 15:34:11** — Clôturer l'ajustement 2.17. « Show Deleted Players » est désormais un switch natif de 44 × 24 px : piste grise désactivée, piste verte activée, curseur blanc animé, survol et focus clavier visibles. Le contrôle conserve son identifiant, son label, `type="checkbox"`, `role="switch"` et son `v-model`, sans nouvelle dépendance. Cypress vérifie les couleurs calculées, la forme arrondie, le déplacement du curseur et le filtrage réel. Validation finale : contrôle TypeScript, lint, 163 tests Vitest répartis dans 23 fichiers, 16 scénarios Cypress et build Vite de production (86 modules) réussissent.
- **2026-08-16 15:03:16** — Clôturer l'ajustement responsive 3.18.1. `SessionForm` et son conteneur scrollable acceptent désormais la contraction avec `min-width: 0`, occupent toute la largeur disponible et masquent tout débordement horizontal ; `ManageSession` adopte le même contrat de largeur flex que Home et Manage Players. Cypress mesure deux cartes de 350 px sur la même ligne à 1280 px, puis une colonne entièrement contenue dans la grille à 375 px. Validation finale : contrôle TypeScript, lint, 162 tests Vitest répartis dans 23 fichiers, 16 scénarios Cypress et build Vite de production (86 modules) réussissent.
- **2026-08-16 12:25:08** — Clôturer la tranche 3.1 à 3.18 consacrée aux participants de Session. Retirer également l'ancien formulaire « Add Player » après démarrage : la composition de `attendingPlayers` ne peut désormais être modifiée que dans `SessionForm` pendant `CREATED`, puis devient immuable en `STARTED` dans l'interface comme dans le domaine et le store. Le scénario Cypress ajouté porte le référentiel à 16 cas et devra disposer d'un miroir Playwright à l'étape 6.12. Validation finale : contrôle TypeScript, lint, 161 tests Vitest répartis dans 23 fichiers, 16 scénarios Cypress, `git diff --check` et build Vite de production (86 modules) réussissent.
- **2026-08-16 12:20:15** — Implémenter la préparation complète de Session : migration contextuelle des anciennes listes, unicité d'une Session ouverte, chargement sans graphe en `CREATED`, persistance de chaque sélection, validation concurrente des statuts, création différée de la première Rotation, composants `SessionPlayerCard`/`SessionForm`, affichage conditionnel dans `ManageSession`, reprise « Continue Setup » depuis Home et protection Delete fondée directement sur `attendingPlayers`. Le store recharge désormais une Rotation à partir du périmètre figé et refuse l'ajout d'un Player extérieur. Validation intermédiaire : contrôle TypeScript, lint et 160 tests Vitest répartis dans 23 fichiers réussissent.
- **2026-08-16 12:06:14** — Implémenter en TDD le cœur du cycle de vie de `Session` : statut initial `CREATED`, `startTime` nullable, sélection persistable de Players uniques et `AVAILABLE`, minimum de quatre participants contrôlé au démarrage, transition unique vers `STARTED` puis vers `FINISHED`, et refus de modifier l'appartenance après démarrage. `attendingPlayers` est exposé par copie afin que sa composition ne puisse changer qu'au travers de l'agrégat. Validation : 8 tests ciblés du modèle et contrôle TypeScript réussissent.
- **2026-08-16 12:03:52** — Valider les règles de préparation : quatre participants minimum sont requis pour démarrer ; une Session `CREATED` est reprise depuis Home avec « Continue Setup » ; chaque sélection est persistée immédiatement dans `attendingPlayers` pendant `CREATED` afin de survivre à un rechargement ; la liste devient immuable en `STARTED`. Pour les anciennes Sessions sans liste, reconstruire les participants depuis le graphe historique, puis utiliser les Players non supprimés uniquement si aucun graphe n'existe. Justification : empêcher une Session inutilisable, préserver le travail de sélection avant démarrage, garantir un unique périmètre de participants et assurer la continuité des données existantes.
- **2026-08-16 00:48:33** — Ajouter l'étape 6.12 de migration partielle de Cypress vers Playwright immédiatement après la modernisation globale de l'outillage en 6.11. Cette décision remplace la qualification de 6.11 comme dernière sous-étape de l'étape 6, sans avancer son calendrier. Réévaluer alors la suite Cypress réellement présente, migrer d'abord les smoke tests de routes et rechargements, puis les parcours métier Manage Players, tout en conservant provisoirement sous Cypress les contrôles géométriques détaillés de Home. Faire coexister les deux runners jusqu'à parité et supprimer chaque scénario Cypress uniquement après validation de son équivalent Playwright. Justification : apprendre et introduire Playwright par incréments vérifiables, limiter le risque d'une réécriture globale et conserver une couverture E2E continue pendant la transition.
- **2026-08-16 00:14:34** — Clôturer l'étape 2 après validation de l'ensemble de `ManagePlayers`, du catalogue persistant, de la compatibilité des statuts historiques, du verrouillage des compositions de Rotation et des parcours directs/rechargés. Les transitions d'exécution de `RotationStatus` restent réservées aux étapes 3 à 5. Justification : livrer un incrément administratif cohérent et testé sans anticiper l'interface de déroulement d'une Rotation. Validation finale : contrôle TypeScript, lint, 138 tests Vitest répartis dans 18 fichiers, 15 parcours Cypress, `git diff --check` et build Vite de production (80 modules) réussissent.
- **2026-08-16 00:12:53** — Remplacer la table et le formulaire intégré de `ManagePlayers` par `CardGrid`, `CreateEntityCard`, `PlayerCard`, `PlayerForm` et deux `BaseModal` contrôlées. La vue détient sélection, recherche accent-insensible, toggle des supprimés et erreurs ; le store détient les mutations persistantes. Les commandes Edit/Delete ou Restore sont rendues conditionnellement selon sélection et statut. `/manage-players` ne déclenche plus `ensureSessionGuard`. Les mutations de composition `setCourts`, `addPlayer`, `removePlayer` et `movePlayer` sont refusées hors `RotationStatus.CREATED`; avant démarrage, le statut reste `AVAILABLE`. Justification : reprendre l'architecture éprouvée de Home, maintenir les composants sans dépendance Pinia et appliquer la règle de verrouillage au niveau du store. Validation intermédiaire : 134 tests Vitest, contrôle TypeScript, lint, `git diff --check` et 15 parcours Cypress réussissent.
- **2026-08-16 00:04:46** — Introduire `usePlayerStore` comme catalogue complet et y centraliser création, édition du nom, suppression logique et restauration. La suppression inspecte les Rotations des Sessions `STARTED`, leurs waiting lists et les Teams référencées par leurs Games ; une référence bloque Delete, tandis qu'un historique `FINISHED` reste intact. `useSessionStore` exclut les Players `DELETED`, conserve `AVAILABLE` pendant `CREATED` et refuse les déplacements dans les autres statuts. `storageService.updatePlayers()` fusionne les mises à jour sans supprimer les enregistrements absents du sous-ensemble courant. Justification : préserver le catalogue et l'historique tout en séparant administration et orchestration de Session. Validation : 27 tests de stores ciblés, contrôle TypeScript et lint réussissent.
- **2026-08-16 00:01:06** — Remplacer `PlayerStatus.ACTIVE` par `PLAYING` et convertir automatiquement l'ancienne valeur sérialisée `ACTIVE` lors de `PlayerBuilder.fromJson()`. Ajouter `RotationStatus` au modèle et au JSON de `Rotation`, avec `CREATED` comme valeur par défaut en construction et lors de la restauration d'un ancien JSON sans statut. Justification : préserver la compatibilité du `localStorage`, distinguer clairement un Player effectivement en jeu d'un Player seulement disponible et préparer les transitions de Rotation sans les coupler encore à l'interface. Validation : 11 tests de modèles ciblés, contrôle TypeScript et lint réussissent.
- **2026-08-15 16:38:01** — Considérer l'étape 1.25 terminée. Après création, l'ancienne carte reçoit `isSelected = false` et la nouvelle reçoit `isSelected = true`, son rail Edit/Delete et son action de Session, sans recherche DOM ni dépendance à l'ordre des Locations. Validation finale : contrôle TypeScript, lint, 98 tests Vitest répartis dans 15 fichiers, 12 parcours Cypress, `git diff --check` et build Vite de production réussissent.
- **2026-08-15 16:28:37** — Considérer l'étape 1.24 terminée. `CreateEntityCard` et `BaseModal` constituent désormais des frontières d'interaction qui empêchent leurs clics de déclencher `clearSelection()` dans `HomeView`, sans modifier la réactivité de `selectedLocationId` ni la désélection par un véritable clic extérieur. Validation finale : contrôle TypeScript, lint, 97 tests Vitest répartis dans 15 fichiers, 11 parcours Cypress, `git diff --check` et build Vite de production réussissent.
- **2026-08-15 16:27:34** — Appliquer le modificateur Vue `.stop` au clic racine de `CreateEntityCard` et à la racine visible de `BaseModal`. Justification : couper la propagation au plus près de la source sans changer `selectedLocationId`, `clearSelection()`, l'événement `activate`, l'événement `close` ni les slots. Validation intermédiaire : 26 tests ciblés, contrôle TypeScript, lint et 11 parcours Cypress réussissent. Le nouveau parcours sélectionne une Location, ouvre Create, interagit avec son formulaire, ferme la modale, ouvre Edit et interagit avec son formulaire sans perdre l'état selected ; le parcours historique de désélection extérieure reste vert.
- **2026-08-15 16:05:14** — Considérer l'étape 1.23 terminée. Les actions Play/Start et Fast Forward/Continue conservent respectivement les noms accessibles complets Start New Session et Manage Current Session ainsi que l'événement `session`. Le rail Edit/Delete et les informations de la Location restent centrés. Validation finale : contrôle TypeScript, lint, 95 tests Vitest répartis dans 15 fichiers, 10 parcours Cypress, `git diff --check` et build Vite de production réussissent.
- **2026-08-15 16:04:24** — Implémenter l'action de Session comme un bouton natif transparent composé d'un SVG circulaire de 52 px et d'un libellé court. Le SVG Play utilise un triangle blanc sur `#42b983`; Fast Forward utilise deux triangles foncés sur `#FFAA1F`. Positionner le bouton à `top: 4px` et `right: 4px`, réserver 5,25 rem en haut de la carte et appliquer `text-align: center` à son contenu. Justification : mesurer les retraits sur le bord visible du cercle, conserver le focus à l'intérieur de la carte et isoler l'action absolue du flux centré. Validation intermédiaire : 31 tests ciblés, contrôle TypeScript, lint et 10 parcours Cypress réussissent ; Cypress vérifie 52 px, le rapport 1,3, les deux retraits de 4 px, les couleurs et les centrages.
- **2026-08-15 15:37:23** — Considérer l'étape 1.22 et la partie fonctionnelle de `HomeView` terminées. La validation complète réussit : contrôle TypeScript, lint, 95 tests Vitest répartis dans 15 fichiers, 10 parcours Cypress et build Vite de production. `git diff --check` ne signale aucune erreur de whitespace ; les avertissements CRLF existants sont informatifs. La modernisation globale de l'outillage reste une tranche distincte à l'étape 1.23.
- **2026-08-15 15:36:23** — Considérer l'étape 1.21 terminée : `canEditCourtCountByLocationId` expose à l'interface la règle « aucune Session STARTED », `LocationForm` désactive uniquement `nbCourts` avec une explication associée par `aria-describedby`, et `useLocationStore.updateLocation()` relit les Sessions persistées puis refuse seulement un changement effectif du nombre de courts. Le nom et la description restent modifiables, l'absence de Session ou un historique entièrement `FINISHED` autorise le changement, et une erreur conserve la modale ouverte. Validation ciblée : 42 tests sur les quatre fichiers concernés, contrôle TypeScript, lint et 10 parcours Cypress réussissent.
- **2026-08-15 15:33:51** — Considérer l'étape 1.20 terminée : `LocationCard` émet l'identifiant, `HomeView` conserve une cible de suppression distincte dans un `shallowRef`, rend une seconde instance de `BaseModal` pour la confirmation, appelle `deleteLocation()` uniquement après validation et efface la sélection correspondante. Justification : conserver la carte indépendante de Pinia et séparer la modale de confirmation de la modale formulaire partagée par Create/Edit. Validation : 17 tests ciblés de `HomeView`, contrôle TypeScript, lint et 9 parcours Cypress réussissent ; le test navigateur confirme la disparition de la Location active et sa conservation persistée en statut `DELETED`.
- **2026-08-15 15:31:51** — Considérer l'étape 1.19 terminée : dériver une classe de variante depuis `startedSessionCount` sans introduire d'état local. Le bouton de Session reprend le padding, le rayon, l'absence de bordure et la graisse du bouton Create de `ManagePlayers`; Start utilise `#42b983` avec texte blanc et Manage `#FFAA1F` avec texte `#2c3e50`. Validation : 11 tests ciblés de `LocationCard`, 13 tests d'intégration de `HomeView`, contrôle TypeScript et lint réussissent. Les scénarios d'édition de `HomeView` reproduisent désormais le parcours sélection puis Edit imposé par l'étape 1.18.
- **2026-08-15 15:30:53** — Considérer l'étape 1.18 terminée : le rail Edit/Delete est conditionné par `v-if="isSelected"`, ce qui le retire du DOM et de l'ordre de tabulation hors sélection. Son positionnement `top: 3px` et son padding réservé le maintiennent entièrement dans la carte au-dessus du titre. Validation : 11 tests ciblés de `LocationCard` et 8 parcours Cypress réussissent, dont la mesure du retrait supérieur, le centrage horizontal et les transitions de visibilité.
- **2026-08-15 15:29:18** — Le développeur valide le découpage des étapes 1.18 à 1.21 et autorise leur réalisation. Exécuter ces tranches séquentiellement avec tests et mise à jour du plan après chacune, en commençant par le rail Edit/Delete. Justification : préserver des changements petits et vérifiables avant d'introduire la confirmation de suppression puis la règle métier transverse.
- **2026-08-15 15:26:09** — Autoriser la modification de `Location.nbCourts` uniquement lorsqu'aucune Session associée n'est `STARTED`, ce qui autorise explicitement l'absence de Session ou un historique composé uniquement de Sessions `FINISHED`. Ne pas se limiter au statut de la dernière Session. Justification : le code détecte déjà comme possible l'incohérence de plusieurs Sessions démarrées ; une Session plus récente `FINISHED` ne doit pas masquer une ancienne Session encore `STARTED`. Appliquer une double protection : désactivation explicative du champ dans `LocationForm` pour l'expérience utilisateur, puis validation centrale dans `useLocationStore.updateLocation()` à partir des Sessions persistées pour empêcher un contournement direct ou une course pendant l'ouverture de la modale. L'implémentation reste suspendue à la validation du plan.
- **2026-08-15 15:03:58** — Considérer l'ajustement de l'étape 1.18 terminé : Edit et Delete sont désormais côte à côte, centrés sur la bordure supérieure de `LocationCard` et séparés du titre par l'espace réservé dans la carte. Les SVG, boutons accessibles, états visuels et événements restent inchangés. Validation finale : contrôle TypeScript, lint, 84 tests Vitest répartis dans 15 fichiers, 8 parcours Cypress et build Vite de production réussissent.
- **2026-08-15 15:03:10** — Positionner le rail de commandes avec `top: 0`, `left: 50%` et une translation de `-50%` sur les deux axes afin que son centre horizontal coïncide avec celui de la carte et que le centre vertical des boutons coïncide exactement avec sa bordure supérieure. Réserver l'espace du titre par le padding supérieur de la carte et supprimer sa marge haute. Justification : obtenir la géométrie demandée sans sortir le titre du flux ni modifier les événements Vue. Validation intermédiaire : 11 tests ciblés de `LocationCard`, contrôle TypeScript, lint et 8 parcours Cypress réussissent ; le test navigateur contrôle le centrage, l'ordre Edit/Delete, l'alignement horizontal et l'absence de chevauchement avec le titre.
- **2026-08-15 15:01:30** — Rouvrir l'étape 1.18 et remplacer la disposition verticale sur le bord droit par un rail horizontal centré sur la bordure supérieure, Edit à gauche et Delete à droite, au-dessus du titre. Cette décision remplace le positionnement consigné à 14:53:27, 14:57:12 et 14:58:16 sans remettre en cause le dessin SVG, l'accessibilité ni les événements des commandes. Justification : ajustement demandé après visualisation de la carte ; l'étape fonctionnelle 1.19 reste différée jusqu'à validation de cette présentation.
- **2026-08-15 14:58:16** — Considérer l'étape 1.18 terminée : `LocationCard` expose désormais deux commandes circulaires à SVG intégré, avec un crayon pour Edit et une poubelle au couvercle entrouvert pour Delete, empilées dans un rail centré sur son bord droit. Le padding réserve l'espace du rail, les commandes restent au-dessus de la surface globale de sélection et les états de survol/focus les distinguent sans modifier leurs événements. Validation finale : contrôle TypeScript, lint, 84 tests Vitest répartis dans 15 fichiers, 8 parcours Cypress et build Vite de production réussissent.
- **2026-08-15 14:57:12** — Pour l'étape 1.18, intégrer directement dans `LocationCard` deux SVG au trait utilisant `currentColor`, les rendre décoratifs au moyen de `aria-hidden` et conserver le nom accessible sur leurs boutons natifs. Isoler ces boutons dans un rail positionné au centre du bord droit, réserver son espace par le padding de la carte et laisser l'action de Session dans le flux normal. Justification : éviter une dépendance graphique pour deux formes simples, préserver les contrats d'événements du composant et empêcher le rail de chevaucher le contenu. Validation intermédiaire : 11 tests ciblés de `LocationCard`, contrôle TypeScript, lint et 8 scénarios Cypress réussissent ; le scénario navigateur vérifie l'ordre, l'alignement, le centrage, le retrait de 12 px et l'absence de chevauchement avec l'action de Session.
- **2026-08-15 14:31:07** — Considérer l'étape 1.17 terminée : la commande Edit de chaque `LocationCard` ouvre la modale commune avec le bon brouillon, les fermetures réinitialisent la cible et la soumission conserve l'identifiant via `updateLocation()`. Validation finale : contrôle TypeScript, lint, 83 tests Vitest répartis dans 15 fichiers, 7 parcours Cypress et build Vite de production réussissent.
- **2026-08-15 14:30:22** — Réutiliser une seule instance de `BaseModal` dans `HomeView` pour la création et l'édition. Conserver la cible d'édition dans un `shallowRef`, dériver le titre, le libellé et la clé du formulaire avec `computed`, puis router la soumission vers `createLocation()` ou `updateLocation()`. Réduire la prop `location` de `LocationForm` à la projection publique `name`/`description`/`nbCourts`. Justification : éviter la duplication de la structure modale, recréer le brouillon à chaque changement de contexte et ne pas faire dépendre un composant de présentation du champ privé `_status` déballé par Pinia. Validation intermédiaire : les 16 tests ciblés de `HomeView` et `LocationForm`, le contrôle TypeScript et le lint réussissent.
- **2026-08-15 14:24:17** — Considérer l'étape 1.16 terminée après validation du parcours d'ouverture, de fermeture, d'annulation et de soumission de la création dans `HomeView`. Validation finale : contrôle TypeScript, lint, 79 tests Vitest répartis dans 15 fichiers, 7 parcours Cypress et build Vite de production réussissent.
- **2026-08-15 14:22:51** — Implémenter le parcours de création en faisant de `HomeView` le propriétaire du `ref` d'ouverture et l'orchestrateur des événements `activate`, `close` et `submit`. Placer `LocationForm` dans le slot principal de `BaseModal` et relier le bouton Create du slot `actions` au formulaire par son attribut HTML `form`. Appeler `locationStore.createLocation()` avant de fermer la modale. Justification : conserver les composants enfants indépendants du store, utiliser la soumission HTML native malgré la séparation en slots et laisser la modale ouverte si la création échoue. Validation intermédiaire : les 9 tests ciblés de `HomeView`, le contrôle TypeScript et le lint réussissent.
- **2026-08-15 14:19:03** — Ajouter l'intégration de l'édition d'une Location en modale comme étape 1.17 distincte de la création. `HomeView` consommera l'événement `edit` de `LocationCard`, conservera la Location ciblée, la transmettra à `LocationForm`, appellera `locationStore.updateLocation()` avec son identifiant et le DTO émis, puis réinitialisera la cible après succès ou annulation. Justification : le store, le formulaire et la carte exposent déjà séparément les contrats nécessaires ; une tranche d'orchestration dédiée permet de vérifier le préremplissage et la conservation de l'identifiant sans complexifier l'incrément de création.
- **2026-08-15 01:37:54** — Considérer l'étape 1.15 terminée : `ensureSession({ locationId, sessionId })` retrouve exactement les deux entités, vérifie leur relation et ne réutilise un graphe déjà en mémoire que s'il correspond aux mêmes identifiants. Le garde de `manageSession` transmet les paramètres de route, tandis que les deux routes historiques conservent l'appel sans argument. `saveSessionGraph()` remplace les Courts de la seule Location courante et ajoute ou remplace par identifiant les autres éléments, en préservant le graphe d'une autre Location couvert par le test. Validation : contrôle de types, lint, 74 tests Vitest, 5 tests Cypress et build Vite réussissent.
- **2026-08-15 01:26:04** — Considérer l'étape 1.14 terminée : `HomeView` choisit une `Session` typée selon la cardinalité, crée uniquement lorsque celle-ci est nulle, réutilise l'unique Session `STARTED` existante et exécute ensuite une commande de navigation commune. Le test dédié vérifie à la fois l'identifiant de route de la Session existante et l'absence d'appel à `createSessionForLocation()`. L'import de `Session` est limité au type et le cas incohérent de cardinalité multiple retourne sans navigation. Validation : contrôle de types, lint, 70 tests Vitest, 5 tests Cypress et build Vite réussissent.
- **2026-08-15 01:22:30** — Intégrer la centralisation du formatage à la modernisation globale de l'outillage planifiée après `HomeView`, plutôt que l'introduire au milieu de l'étape 1. Utiliser des fichiers de configuration versionnés et des dépendances locales verrouillées afin que le formatage ne dépende pas des préférences personnelles d'IntelliJ IDEA. Après validation des règles partagées, reformater tout le projet dans un lot mécanique distinct et exiger une vérification non mutante du format dans la chaîne automatisée. Justification : garantir les mêmes règles à tous les développeurs et éditeurs, rendre les écarts détectables automatiquement et préserver la lisibilité de l'historique en séparant formatage global et changements fonctionnels.
- **2026-08-15 00:50:06** — Considérer l'étape 1.13 terminée : le routeur résout désormais la route nommée `manageSession` avec `locationId` et `sessionId`, et `HomeView` navigue vers cette route en utilisant la Session effectivement retournée par `createSessionForLocation()`. Le test de vue substitue `useRouter()` afin de vérifier la commande de navigation, tandis qu'un test du routeur couvre séparément le contrat d'URL. Validation : contrôle de types, lint, 69 tests Vitest, 5 tests Cypress et build Vite réussissent.
- **2026-08-15 00:46:02** — Pour terminer l'étape 1.13, enregistrer la route nommée `manageSession` sous `/manage/:locationId/:sessionId` et faire naviguer `HomeView` avec les paramètres nommés issus de la Session retournée par `createSessionForLocation()`. Ne pas appliquer à cette route l'ancien garde `ensureSessionGuard`, car celui-ci ignore encore les paramètres et charge le graphe de la Location par défaut ; son adaptation reste isolée à l'étape 1.15. Justification : rendre explicite et testable le contrat d'URL sans introduire prématurément un chargement incorrect de Session.
- **2026-08-15 00:40:39** — Valider la centralisation de la numérotation : `ensureSession()` délègue maintenant à `createSessionForLocation()` lorsqu'aucune Session `STARTED` n'existe. Le test de régression restaure les ordres terminés 1 et 2 pour la Location `default` et vérifie la création de l'ordre 3 ; le test de création Home illustre désormais la suite 1, 2, 3, 4 tout en ignorant l'ordre 9 d'une autre Location. Validation : contrôle de types, lint, 68 tests Vitest, 5 tests Cypress et build Vite réussissent.
- **2026-08-15 00:25:49** — Valider la sous-étape de création de 1.13 : l'action refuse une seconde Session `STARTED`, ignore les ordres des autres Locations, utilise le maximum historique de la Location ciblée, persiste la nouvelle Session, recharge l'état réactif et retourne l'instance créée. `HomeView` consomme l'événement public de `LocationCard` et appelle l'action uniquement lorsque la cardinalité active est zéro. Validation : contrôle de types, lint, 67 tests Vitest, 5 tests Cypress et build Vite réussissent.
- **2026-08-15 00:16:36** — Considérer l'étape 1.12 terminée : `useSessionStore` restaure une collection réactive de Sessions et son getter paramétré ne retient que les Sessions `STARTED` de la Location demandée ; `HomeView` transmet la cardinalité et `LocationCard` rend les deux actions attendues ou une alerte bloquante en cas de cardinalité multiple. Validation : contrôle de types, lint, 63 tests Vitest, 5 tests Cypress et build Vite réussissent.
- **2026-08-15 00:10:33** — Valider l'ajustement visuel de Home : `CardGrid` centre des colonnes limitées à 350 px, les deux types de cartes mesurent effectivement 350 px dans le navigateur et `LocationCard` anime son arrière-plan vers `#f5f5f5` au survol. Validation : contrôle de types, lint, 58 tests Vitest, 5 tests Cypress et build Vite réussissent.
- **2026-08-15 00:02:13** — Distinguer la racine `.app` de `App.vue` du conteneur de montage `#app` conservé par Vue 3. Justification : réutiliser `id="app"` dans le composant créait deux identifiants imbriqués et appliquait deux fois la hauteur minimale. Durcir le test Cypress pour exiger que le bas de `.home` coïncide avec celui du viewport et pas seulement qu'il l'atteigne ou le dépasse. Validation finale : contrôle de types, lint, 58 tests Vitest, 4 tests Cypress et build Vite réussissent.
- **2026-08-14 23:57:22** — Valider une première correction de la couverture verticale de `HomeView` en transformant le layout racine en conteneur Flex d'au moins `100vh`, en incluant son espacement supérieur dans cette hauteur, en portant le contenu routé par un élément `<main>` extensible et en faisant occuper à `.home` l'espace restant. Le scénario Cypress sélectionne une Location, vérifie que `.home` atteint le bas du viewport, clique à cet endroit et constate la disparition de l'action de session. Validation : contrôle de types, lint, 58 tests Vitest, 4 tests Cypress et build Vite réussissent.
- **2026-08-14 23:54:46** — Rouvrir l'étape 1.11 après constat que la surface de `HomeView` s'arrête à la hauteur de son contenu : un clic dans la zone blanche située sous son élément racine ne peut pas atteindre `@click="clearSelection"`. Étendre la chaîne de layout `#app` → contenu routé → `.home` sur la hauteur du viewport et couvrir cette géométrie par Cypress. Justification : un événement DOM ne remonte qu'à travers les ancêtres de sa cible ; agrandir seulement `html` ou `body` ne rendrait pas la zone extérieure descendante de `HomeView`.
- **2026-08-14 23:43:14** — Valider la correction de sélection : `.location-card--selected` fournit un contour persistant distinct du bouton `.location-card-select`, les clics internes restent encapsulés par la carte et tout clic extérieur dans `HomeView` remet `selectedLocationId` à `null`, masque l'action de session et réinitialise `aria-pressed`. Validation : contrôle de types, lint, 58 tests Vitest, 3 parcours Cypress et build Vite réussissent.
- **2026-08-14 23:24:02** — Valider le renommage en `LocationCardModel` et la sélection locale réactive dans `HomeView` : aucune carte n'est sélectionnée initialement, `aria-pressed` et l'unique action de session suivent `selectedLocationId`, et `LocationCard` émet un événement `session` sans connaître la navigation ni le store. Validation : contrôle de types, lint, 58 tests Vitest, 3 parcours Cypress et build Vite réussissent.
- **2026-08-14 23:02:44** — Composer `HomeView` avec `CardGrid`, une `CreateEntityCard` en première position et une `LocationCard` par élément du store, identifié par `location.id`. Charger les Locations au montage de la vue et retirer l'ancien lien direct vers `/manage`. Typer explicitement l'état JavaScript du store comme `Location[]` et limiter la prop de présentation de `LocationCard` aux quatre propriétés publiques consommées via `Pick`, afin de rester compatible avec le déballage réactif Pinia sans dépendre du champ privé du modèle. Validation : contrôle de types, lint, 56 tests Vitest, 3 parcours Cypress et build Vite réussissent.
- **2026-08-14 22:45:59** — Ajouter à `LocationCard` une commande Delete native et contextualisée, émettant uniquement `delete` avec l'identifiant métier ; la confirmation et la suppression logique restent sous la responsabilité du parent. Considérer ensuite `LocationCard` terminé avec ses trois événements publics indépendants `select`, `edit` et `delete`. Validation : contrôle de types, lint, 56 tests Vitest, 3 parcours Cypress et build Vite réussissent.
- **2026-08-14 22:42:49** — Ajouter à `LocationCard` une commande Edit native et contextualisée pour l'accessibilité, émettant uniquement `edit` avec l'identifiant métier. Placer les commandes dans un conteneur positionné au-dessus de la surface de sélection étendue. Justification : conserver le composant sans store, laisser l'ouverture de la modale au parent et isoler naturellement sélection et édition sans modificateur de propagation. Validation intermédiaire : contrôle de types, lint et 55 tests Vitest réussissent.
- **2026-08-14 22:37:34** — Valider le refactoring de la sélection de `LocationCard` vers un bouton natif dont le pseudo-élément couvre la carte et dont le focus visible entoure cette surface. Les tests clavier propres à l'ancien élément ARIA simulé sont remplacés par la vérification du type natif du bouton et de son événement public. Validation : contrôle de types, lint, 54 tests Vitest, 3 parcours Cypress et build Vite réussissent.
- **2026-08-14 22:27:45** — Valider l'incrément de sélection accessible de `LocationCard` : la carte émet `select` avec l'identifiant métier au clic, avec Entrée et avec Espace ; le focus clavier est visible et les événements clavier sont limités à la carte elle-même afin de préparer l'ajout de commandes internes. Validation : contrôle de types, lint, 56 tests Vitest, 3 parcours Cypress et build Vite réussissent.
- **2026-08-14 22:04:01** — Couvrir la mise à jour du libellé du nombre de terrains lorsque le parent remplace la prop `location`, au moyen de `wrapper.setProps`. Justification : vérifier le contrat réactif public de `LocationCard` plutôt que la seule valeur calculée au montage. Validation intermédiaire : les 4 tests ciblés, le contrôle de types et le lint réussissent.
- **2026-08-14 20:31:00** — Conserver l'objet `props` et l'accès explicite `props.location` dans le calcul de `courtLabel`, plutôt que déstructurer la prop. Justification : préférence explicite de l'utilisateur pour rendre visible l'origine de la donnée ; les deux formes préserveraient ici la réactivité avec Vue 3.5. Revue : les 3 tests ciblés, le contrôle de types et le lint réussissent ; la hauteur minimale est correctement appliquée à la description.
- **2026-08-14 20:20:08** — Conserver systématiquement le paragraphe de description de `LocationCard`, même vide, afin de préserver l'alignement visuel des cartes. Exposer un identifiant DOM dérivé de l'identifiant métier et calculer le libellé `court`/`courts` selon `nbCourts`. Validation intermédiaire : les 3 tests ciblés, le contrôle de types et le lint réussissent.
- **2026-08-14 19:49:46** — Valider le premier incrément de rendu de `LocationCard` : prop `Location` obligatoire, structure sémantique en `article` et affichage des trois informations sans dépendance au store. Validation intermédiaire : contrôle de types, lint et test ciblé réussissent. La revue relève le cas singulier `1 court` et la description vide à traiter avant les interactions.
- **2026-08-14 19:34:43** — Considérer l'étape 0.9 terminée après exécution réussie de la chaîne `type-check` → lint → 49 tests Vitest → 3 parcours Cypress → build Vite. Justification : le contrôle des types TypeScript et des templates Vue fait désormais partie du garde automatique précédant tout build de production.
- **2026-08-14 19:34:04** — Installer `vue-tsc` 3.3.9 comme dépendance de développement, exposer `npm run type-check` sous la forme `vue-tsc --noEmit` et l'exécuter en première position dans `prebuild`. Justification : Vite transpile TypeScript sans contrôler complètement les types ni les templates Vue ; le contrôle doit échouer avant les validations plus longues sans générer de fichiers. Compatibilité vérifiée avec TypeScript 5.9.3 et premier contrôle de types réussi.
- **2026-08-14 19:29:24** — Considérer l'étape 1.8 terminée : `LocationForm` maintient un brouillon local en création et en édition, normalise les chaînes avec `v-model.trim`, convertit `nbCourts` avec `v-model.number` et émet un DTO `LocationFormData` typé lors de la soumission sans connaître le store. Validation : 49 tests Vitest, lint, 3 parcours Cypress et build Vite réussissent. Aucun script `type-check` ni exécutable local `vue-tsc` n'est actuellement disponible dans le projet.
- **2026-08-14 18:52:52** — Valider l'initialisation du mode édition de `LocationForm` au moyen d'une prop `location` optionnelle et d'une copie de ses trois valeurs dans le brouillon réactif local. La saisie modifie le brouillon sans muter la `Location` reçue. Validation intermédiaire : les 2 tests ciblés de `LocationForm` réussissent.
- **2026-08-14 18:33:10** — Valider la première tranche de `LocationForm` : le brouillon réactif de création initialise `name` et `description` à vide et `nbCourts` à 1 ; les trois champs exposent les contraintes HTML prévues. Validation intermédiaire : le test ciblé de `LocationForm` réussit.
- **2026-08-14 17:52:49** — Concevoir `LocationForm` autour d'un brouillon local distinct de la `Location` immuable reçue en prop optionnelle. En création, les valeurs initiales seront un nom et une description vides ainsi qu'un `nbCourts` égal à 1 ; en édition, elles seront copiées depuis la Location. Le formulaire recevra un `formId` explicite afin qu'un bouton Submit placé dans le slot `actions` de `BaseModal` puisse lui être associé avec l'attribut HTML `form`, puis émettra des données simples sans persister directement. Justification : éviter toute mutation de prop, partager le même composant entre création et édition, conserver le store comme orchestrateur du cas d'usage et le builder comme autorité finale des invariants.
- **2026-08-14 17:50:55** — Utiliser l'élément natif `<dialog open>` dans `BaseModal`, tout en conservant `v-if="isOpen"` comme mécanisme contrôlé de montage et démontage. Justification : bénéficier de la sémantique HTML du dialogue, rendre effectivement visible l'élément natif et conserver un flux simple où le parent reste propriétaire de l'état ; l'intégration impérative de `showModal()`, du top layer et de la gestion avancée du focus est différée. Validation : 46 tests Vitest, lint, contrôle TypeScript et build Vite réussissent.
- **2026-08-14 13:35:42** — Considérer l'alignement immédiat de l'outillage et `CreateEntityCard` terminés avec ESLint `8.57.0`, `@typescript-eslint` `8.67.0`, `eslint-plugin-vue` `9.33.0` et `vue-eslint-parser` `9.4.3`. L'ancien environnement `vue/setup-compiler-macros` est retiré, les macros TypeScript étant reconnues nativement. Validation : arbre de dépendances cohérent, lint sans warning de compatibilité TypeScript, 43 tests Vitest, contrôle TypeScript et build Vite réussissent. Le warning npm relatif à `min-release-age`, indépendant d'ESLint, reste hors de cette tranche.
- **2026-08-14 13:23:46** — Inclure `eslint-plugin-vue` majeur 9 dans l'alignement immédiat. Justification : après le passage à `@typescript-eslint` 8, `eslint-plugin-vue` 8.7.1 et `vue-eslint-parser` 8.3.0 signalent à tort que les props et événements typés de `CreateEntityCard` ne sont pas définis ; la génération 9 reconnaît nativement les macros de `<script setup>` et permet de retirer l'ancien environnement `vue/setup-compiler-macros`.
- **2026-08-14 13:21:58** — Supprimer dès maintenant le warning de compatibilité TypeScript en mettant à jour ESLint vers `8.57.0` et `@typescript-eslint/parser`/`eslint-plugin` vers la version majeure 8 compatible avec TypeScript `5.9.3`. Après validation de `HomeView`, planifier une seconde phase distincte qui inventoriera et migrera tout l'outillage du projet vers ses versions stables courantes respectives. Cette décision remplace la décision intermédiaire du 2026-08-14 13:19:09 qui acceptait temporairement le warning. Justification : conserver un lint officiellement compatible dès l'introduction de TypeScript dans les composants, tout en différant la migration globale et potentiellement plus disruptive de la chaîne de développement.
- **2026-08-14 13:19:09** — Scinder la modernisation de l'outillage ESLint en deux temps. Mettre d'abord à jour uniquement ESLint de `7.32.0` vers `8.57.0`, en conservant provisoirement `.eslintrc.js` et les plugins actuels ; après l'implémentation de `HomeView`, migrer ESLint vers sa version stable courante et aligner l'ensemble `@typescript-eslint`, `eslint-plugin-vue` et `vue-eslint-parser`, avec migration vers la configuration plate si nécessaire. Cette décision remplace la proposition de mise à jour groupée immédiate d'ESLint et de `@typescript-eslint`. Justification : limiter chaque changement d'outillage à une tranche vérifiable, tout en acceptant explicitement que le warning de compatibilité TypeScript subsiste jusqu'à la migration complète.
- **2026-08-14 12:37:00** — Considérer l'étape 1.5 terminée avec `CardGrid`, dont le slot par défaut conserve le contenu et l'ordre fournis par le parent, et dont le CSS Grid adapte automatiquement ses colonnes à la largeur disponible. Validation : 42 tests Vitest, lint, contrôle TypeScript et build Vite réussissent.
- **2026-08-14 03:09:47** — Considérer l'étape 1.4 terminée après validation du statut par défaut, de la compatibilité des anciennes données, de la transition vers `DELETED`, du filtrage actif et de la conservation historique. Validation : 41 tests Vitest, lint, contrôle TypeScript et build Vite réussissent.
- **2026-08-14 02:51:37** — Considérer l'étape 1.3 terminée après couverture de l'erreur d'identifiant inexistant sans mutation du state ni de la persistance. Validation : 39 tests Vitest, lint, contrôle TypeScript et build Vite réussissent.
- **2026-08-14 02:51:37** — Implémenter la suppression logique au moyen d'un registre persistant d'identifiants supprimés, distinct des objets `Location`. `getLocations()` conserve toutes les Locations pour l'historique ; `getActiveLocations()` filtre les tombstones pour l'affichage courant. Justification : conserver le modèle métier confirmé et les références de Sessions tout en encapsulant la politique de visibilité dans la couche de persistance ; la future implémentation SQLite pourra traduire ce contrat par une colonne de suppression logique.
- **2026-08-14 02:41:38** — Valider le chemin nominal de l'édition : l'objet immuable est reconstruit avec le même identifiant, l'enregistrement ciblé est remplacé sans affecter les autres Locations et le state est rechargé. Validation intermédiaire : 38 tests Vitest, lint et contrôle TypeScript réussissent.
- **2026-08-14 02:32:47** — Exposer l'édition sous la forme `updateLocation({ id, name, description, nbCourts })`, reconstruire une `Location` immuable avec le même identifiant, déléguer son remplacement par identifiant à `storageService.updateLocation()`, puis recharger le state. Justification : préserver l'identité métier, réappliquer les invariants du builder et garder la persistance comme source de vérité après la mutation.
- **2026-08-14 02:31:29** — Considérer le refactoring de création terminé après ajout d'un test prouvant qu'une Location déjà persistée mais absente du state n'est pas écrasée. Validation : 37 tests Vitest, lint, contrôle TypeScript et build Vite réussissent.
- **2026-08-14 02:16:23** — Considérer l'introduction de `LocationBuilder` et l'adaptation de la création terminées après migration de la restauration, du store des Locations et du store de Session. Validation : 36 tests Vitest, lint, contrôle TypeScript et build Vite réussissent.
- **2026-08-14 02:09:09** — Imposer un Fluent `LocationBuilder` comme unique porte d'entrée de construction et de restauration d'une `Location`. Le nom et la description sont trimmés, un nom vide après normalisation est refusé, une description vide après normalisation est acceptée, et `nbCourts` doit être un entier compris entre 1 et 50 inclus. Justification : centraliser les invariants, empêcher leur contournement par un constructeur direct et aligner le modèle sur le pattern déjà employé pour `Player`.
- **2026-08-14 01:49:40** — Exposer la création sous la forme `createLocation({ name, description, nbCourts })` et construire le modèle `Location` dans l'action Pinia. Justification : laisser le futur formulaire émettre des données simples, centraliser le cas d'usage et garantir que les validations du constructeur métier sont toujours exécutées.
- **2026-08-14 01:49:01** — Faire charger le store des Locations en remplaçant sa collection réactive par les instances restaurées par `storageService.getLocations()`. Justification : garder le store indépendant des clés de persistance, rendre le chargement idempotent et éviter les doublons lors d'appels répétés. Validation : 23 tests Vitest et le lint réussissent.
- **2026-08-13 19:49:46** — Considérer l'étape 0.7 terminée après validation de 22 tests Vitest répartis dans 7 fichiers, du lint, des trois parcours Cypress et du build Vite. Justification : un rendu simple, une dépendance de routage substituée, un état Pinia contrôlé et une interaction utilisateur disposent désormais d'exemples vérifiés.
- **2026-08-13 19:20:41** — Utiliser Vue Test Utils pour monter les composants dans `happy-dom`, substituer les dépendances hors périmètre et vérifier le DOM ainsi que les interactions publiques. Justification : tester le contrat visible des composants sans lancer un navigateur complet ni coupler les assertions à leurs méthodes internes.
- **2026-08-13 18:29:41** — Considérer l'étape 0.6 terminée après validation de 19 tests Vitest répartis dans 5 fichiers, du lint, des trois parcours Cypress et du build de production. Justification : les invariants et sérialisations des modèles principaux ainsi que l'initialisation, l'ajout et l'affectation des joueurs dans le store disposent désormais d'un socle de non-régression automatisé.
- **2026-08-13 18:29:41** — Installer Vitest 4.1.10 et `happy-dom` 20.11.2, partager le plugin Vue et l'alias `@` depuis `vite.config.mjs`, et exécuter les tests unitaires avant Cypress dans le hook `prebuild`. Justification : réutiliser exactement la chaîne de transformation Vite, fournir `localStorage` uniquement aux tests qui en dépendent et empêcher un build lorsque les règles métier régressent.
- **2026-08-12 19:56:21** — Considérer la migration Vue CLI → Vite terminée après validation du lint, des trois parcours Cypress et du build de production Vite. Justification : les accès directs et rechargements de `/`, `/manage` et `/manage-players` fonctionnent, et le bundle de production est généré sans erreur ni avertissement Vite.
- **2026-08-12 19:52:02** — Adopter la configuration Vite native avec `index.html` à la racine, `@vitejs/plugin-vue`, l'alias `@` explicite et `import.meta.env.BASE_URL`, tout en conservant `npm run serve` comme alias de compatibilité vers Vite. Justification : suivre les conventions Vite sans imposer immédiatement un changement d'habitude, préserver le déploiement sous un chemin de base et limiter la migration à la chaîne de build.
- **2026-08-12 18:47:46** — Utiliser Vitest pour les tests unitaires et les tests de composants Vue, tout en conservant Cypress pour les parcours E2E. Cette décision remplace la partie de la décision du 2026-08-12 02:22:35 qui réservait les futurs tests de composants à Cypress. Justification : unifier les transformations Vite/Vitest pour les tests rapides et isolés, et réserver Cypress à la validation de l'application complète dans le navigateur.
- **2026-08-12 18:47:46** — Remplacer Vue CLI par Vite avant d'introduire les nouveaux tests. Justification : Vue CLI est en maintenance, Vite est la chaîne de build recommandée pour Vue 3 et sa configuration pourra ensuite être partagée directement avec Vitest.
- **2026-08-12 02:55:26** — Retirer les dépendances directes `vuex` et `@vue/cli-plugin-vuex`, tout en conservant l'occurrence transitive du plugin imposée par `@vue/cli-service@5.0.9`. Justification : le runtime Vuex et sa déclaration projet ont entièrement disparu ; éliminer également le plugin transitif nécessiterait une migration distincte hors de Vue CLI. Le lint, les trois tests Cypress de routes et le build de production réussissent après ce retrait.
- **2026-08-12 02:51:58** — Supprimer `src/store/index.js` après avoir migré tous ses consommateurs vers `useSessionStore`. Justification : l'ancien store Vuex ne faisait plus partie du graphe de modules actif ; le lint, les trois tests Cypress de routes et le build de production confirment que Pinia est désormais l'unique store utilisé par l'application.
- **2026-08-12 02:27:17** — Exécuter automatiquement le lint et les smoke tests Cypress des routes via le hook npm `prebuild`. Justification : empêcher la production d'un build lorsque l'accès direct, le rechargement d'une route ou une ressource HTTP de l'application est en erreur.
- **2026-08-12 00:37:27** — Initialiser temporairement Pinia et Vuex dans le garde de la route `/manage` jusqu'à la migration de `ManageSession`. Justification : le composant consomme encore Vuex ; supprimer immédiatement son initialisation rendrait son état vide, tandis que cette transition permet de vérifier chaque modification séparément.
- **2026-08-10 01:41:38** — Considérer `SETUP` comme une étape d'interface et non comme un statut métier persistant. Justification : `SETUP` décrit la validation et l'orientation de la vue avant le chargement d'une Session.
